<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
<% @urls.each do |entry| %>
  <url>
    <loc><%= h entry[:loc] %></loc>
<% if entry[:lastmod].present? %>
    <lastmod><%= h entry[:lastmod] %></lastmod>
<% end %>
  </url>
<% end %>
</urlset>
