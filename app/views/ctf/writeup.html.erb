<% content_for :head do %>
  <% writeup_section = @ctf_info["ctf"].presence || @ctf_name %>
  <% writeup_title = "#{@ctf_info["title"]} - #{writeup_section} writeup" %>
  <% writeup_description = @ctf_info["description"].presence || "Writeup for #{@ctf_info["title"]} from #{@ctf_info["year"]} CTF." %>
  <% writeup_tags = (Array(@ctf_info["categories"]) + [ @ctf_name ]).map(&:to_s).reject(&:blank?).uniq %>
  <% writeup_modified = @ctf_info["updated"].presence || @ctf_info["modified"].presence %>
  <% writeup_authors = writeup_authors(@ctf_info) %>
  <%= seo_meta_tags(
        title: writeup_title,
        description: writeup_description,
        type: "article",
        canonical_path: request.path,
        image: @ctf["logo"],
        published_time: @ctf_info["published"],
        modified_time: writeup_modified,
        tags: writeup_tags,
        section: writeup_section,
        authors: writeup_authors,
        json_ld: seo_article_schema(
          title: writeup_title,
          description: writeup_description,
          canonical_path: request.path,
          published: @ctf_info["published"],
          modified: writeup_modified,
          tags: writeup_tags,
          image: @ctf["logo"],
          section: writeup_section,
          authors: writeup_authors
        )
      ) %>
<% end %>

<%= render_taskbar_items %>
<%= render_terminal([], true) %>

<main class="content-page article-page">
<div class="writeup-wrapper relative flex items-start gap-4 mx-4">
  <div class="writeup-container bg-primary">
    <button
      type="button"
      class="collapse-toc-button toc-toggle-button writeup-toc-restore-button"
      title="Show table of contents"
      aria-label="Show table of contents"
      data-toc-toggle="toc"
      aria-expanded="true"
      aria-controls="toc"
    >
      <svg
        class="w-5 h-5"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round">
        <path d="M9 6l6 6-6 6"/>
      </svg>
    </button>

    <% writeup_ctf_label = "#{(@ctf_name.presence || @which).to_s.upcase}-#{@ctf_info["year"]}" %>
    <h4 class="writeup-year text-white text-center">
      <% if @ctf["website"].present? %>
        <%= link_to writeup_ctf_label,
                    @ctf["website"],
                    class: "writeup-year-link",
                    target: "_blank",
                    rel: "noopener noreferrer" %>
      <% else %>
        <%= writeup_ctf_label %>
      <% end %>
    </h4>
    <h1 class="writeup-title text-white text-center"><%= @ctf_info["title"] %></h1>
    <%= render "shared/post_meta", items: [ @ctf_info["published"] ], reading_time: @ctf_info["reading_time_label"] %>

    <% article_badges = [
         render_writeup_difficulty_badge(@ctf_info, context: :article),
         render_writeup_winner_badge(@ctf_info, context: :article),
         render_writeup_authored_badge(@ctf_info, context: :article)
       ].compact %>
    <% if article_badges.any? %>
      <div class="writeup-winner-article writeup-badges-article">
        <%= safe_join(article_badges) %>
      </div>
    <% end %>

    <div class="flex flex-col items-center gap-4">
      <div class="writeup-challengefiles flex items-center gap-4">
        <% if @challenge_file %>
          <%= link_to ctf_file_download_path(@challenge_file[:relative_path]),
                      class: "download-btn ui-article-action mt-3",
                      target: "_blank",
                      rel: "noopener",
                      aria: { label: "Download #{@challenge_file[:basename]}" } do %>
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M12 3v12" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M8 11l4 4 4-4" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M21 21H3" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <%= @challenge_file[:basename] %> - <%= number_to_human_size(@challenge_file[:size]) %>
          <% end %>
        <% end %>

        <% if @pdf_writeup %>
          <%= link_to "/ctf/writeups/#{@pdf_writeup[:relative_path]}",
                      class: "open-pdf-btn ui-article-action mt-3",
                      target: "_blank",
                      rel: "noopener",
                      aria: { label: "PDF Writeup" } do %>
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M6 19a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2h7l5 5v9a2 2 0 0 1-2 2H6z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M13 7V2.5L18.5 8H14a1 1 0 0 1-1-1z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            PDF Writeup
          <% end %>
        <% end %>
      </div>
    </div>

    <%= @html_content %>

    <div class="next-previous-writeups flex justify-between my-8">
      <% if @previous_writeup %>
        <a
          href="<%= @previous_writeup[:link] %>"
          class="previous-writeup-btn ui-article-action mt-3"
        >
          <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M15 18l-6-6 6-6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          <span><%= @previous_writeup[:which].upcase %> - <%= @previous_writeup[:title] %></span>
        </a>
      <% else %>
        <div></div>
      <% end %>

      <% if @next_writeup %>
        <a
          href="<%= @next_writeup[:link] %>"
          class="next-writeup-btn ui-article-action mt-3"
        >
          <span><%= @next_writeup[:which].upcase %> - <%= @next_writeup[:title] %></span>
          <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M9 6l6 6-6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      <% else %>
        <div></div>
      <% end %>
    </div>
  </div>

  <div id="toc" class="table-of-content bg-primary">
    <div class="toc-header">
      <h3 class="text-slate-100 font-semibold text-3xl">Table of contents</h3>
      <button
        id="toc-toggle"
        type="button"
        class="collapse-toc-button toc-toggle-button"
        title="Collapse table of contents"
        aria-label="Collapse table of contents"
        data-toc-toggle="toc"
        aria-expanded="true"
        aria-controls="toc"
      >
        <svg
          id="toc-toggle-icon"
          class="w-5 h-5"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round">
          <path d="M9 6l6 6-6 6"/>
        </svg>
      </button>
    </div>
    <ul id="toc-body" class="text-slate-100">
      <% @headings.each do |heading| %>
        <% toc_depth = [[ heading[:depth].to_i, 0 ].max, 4].min %>
        <li class="toc-item toc-depth-<%= toc_depth %>" style="margin-left: <%= toc_depth * 0.85 %>rem;">
          <a href="#<%= heading[:anchor] %>" class="toc-anchor group flex items-start py-1">
            <% if toc_depth.positive? %>
              <svg width="3" height="24" viewBox="0 -9 3 24"
                class="toc-indent-marker mr-2 text-slate-400 overflow-visible group-hover:text-slate-600 dark:text-slate-600 dark:group-hover:text-slate-500">
                <path d="M0 0L3 3L0 6" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">
                </path>
              </svg>
            <% end %>
            <%= heading[:text] %>
          </a>
        </li>
      <% end %>
    </ul>
    <%= render "shared/post_progress", word_total: @ctf_info["word_count"] %>
  </div>
</div>
</main>
